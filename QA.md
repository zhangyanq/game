# 验证记录 · 2026-09-10

## 已通过

- `node --test tests/*.test.js`：46/46。
- 500 个初始棋盘：没有开场自动匹配，且至少有一个合法交换。
- 30 关各 4 次随机走法模拟：步数正确消耗，棋盘状态有效，均能结算胜负。
- 四连、五连、T/L 特殊棋子、双彩虹、清沙和救鱼目标检查。
- 自然恢复、离线时间、每日补给、溢出体力、北京时间跨日、时钟回拨基础保护。
- 广告完整观看/取消/错误/重复请求 mock，未配置广告位不模拟奖励。
- 通关奖励只结算一次，失败只扣一次体力，进行中棋盘保存/恢复，零体力限制开局。
- 实际 `game.js` 在无 DOM 的 wx mock 环境启动并绘制一帧。
- Canvas 原生绘制及命中检查：320×568、375×667、390×844、430×932。
- 内置浏览器实际打开预览并检查棋盘、提示、交换消除和步数/分数变化。

## 本轮规则与动画验证

- 同向直线不误触发另一行/列；异向十字以交换目标格为中心。
- 直线+爆炸按来源方向四线扩散；双爆炸精确菱形半径 4。
- 交叉匹配魔力鸟优先；六连七连生成魔力鸟。
- 魔力鸟+普通/直线/爆炸/魔力鸟的转化与影响范围。
- 银币阻挡，满足清除条件后同波放行，特效顺序不影响结果。
- 冰层逐层减少、藤蔓禁止移动/下落、鸡窝产出、传送带位移、染色蛋。
- 无效交换动画后状态完全不变；中间帧两枚棋子确实绘制在格子之间。
- 浏览器实际交换后步数与得分正确变化，检查时无浏览器错误日志。
- 30 关各 4 次随机模拟完成胜负结算；这不是真人通关率，也不代表关卡平衡已经定稿。

## 尚未验证

- 微信开发者工具实际编译、上传和小游戏后台审核。
- iOS/Android 微信真机的音效、震动、性能、胶囊和安全区域。
- 真实激励广告（尚未提供广告位 ID）。
- 服务端防作弊与云端存档（当前版本未实现）。

图像预览由同一 Canvas 场景代码渲染，并非微信真机截图。浏览器预览也不代替微信真机验收。

## v0.3 音乐检查

- 新增单例循环播放器、低音量、暂停恢复和中断后保持关闭状态的测试。
- 浏览器设置页显示独立背景音乐开关，检查时无错误日志。
- WAV 为 48 秒、22.05 kHz、16-bit 单声道，约 2.12 MB。
- 未完成微信真机听感与系统静音模式验证。

## v0.4 清屏验证

新增每日两次/全局五次/跨日重置、全屏障碍清除且不扣步数、广告成功发奖、广告取消不改变棋盘与次数的测试；浏览器已检查按钮布局。真实广告尚待接入验证。
