# 规则实现说明 · v0.2

以下为本次确认后已实现的规则。游戏中的「彩虹珍珠」对应规则表里的「魔力鸟」，图鉴使用两者的对应说明。

## 基础与生成优先级

- 相邻交换形成横向/竖向三连消除；无效交换会回弹，不扣步数。
- 四连生成对应方向的直线特效。
- 五个同色形成 T/L 型，生成爆炸特效；单枚爆炸影响周围 3×3。
- 直线五连及以上生成魔力鸟；六连、七连不会生成更高级特效。
- 相互交叉的一组匹配只生成一个特效，优先级为魔力鸟 > 爆炸 > 直线。互不相连的匹配可以各自生成特效。
- 优先在玩家交换后的目标格生成；不适用时使用对应匹配组的中心或交叉格。

## 特效组合

| 组合              | 已实现效果                                                              |
| ----------------- | ----------------------------------------------------------------------- |
| 横直线 + 横直线   | 以交换目标格为准，触发一次整行攻击                                      |
| 竖直线 + 竖直线   | 以交换目标格为准，触发一次整列攻击                                      |
| 横直线 + 竖直线   | 交换目标格为中心，触发一行一列十字攻击                                  |
| 直线 + 爆炸       | 连续四行/四列攻击；朝交换来源方向扩散两条，另一方向扩散一条，另含中心线 |
| 爆炸 + 爆炸       | 用户确认的菱形：横纵距离之和不超过 4，越界部分截断                      |
| 魔力鸟 + 普通动物 | 消除所有对应颜色的动物，并消耗参与交换的魔力鸟                          |
| 魔力鸟 + 直线     | 所有同色动物转为该方向直线，并立即触发                                  |
| 魔力鸟 + 爆炸     | 所有同色动物转为爆炸，并立即触发                                        |
| 魔力鸟 + 魔力鸟   | 先计算并触发现有全部特效，再全屏清除；视觉分为引爆、清屏两个阶段        |

直线+爆炸中，如果交换方向恰好与直线方向一致，则没有垂直方向的来源侧，此时固定向上/左多扩散一条。两个参与组合的棋子均被消耗，不能因此重复触发另一条同向直线。

## 银币、连锁与死局

- 银币是固定障碍，不可直接交换，也不下落。
- 本次补充的银币清除条件：相邻动物被普通匹配消除，或银币被爆炸/全屏效果覆盖。
- 单独的直线攻击到银币前停止，不会单靠直线清除银币。
- 同一波满足清除条件的银币先被标记清除，再计算直线通路；结果不依赖特效遍历顺序。
- 动物消除后，按照各列及固定障碍分隔出的区段下落、顶部补充；新匹配继续形成特效和连锁。
- 没有任何合法交换时洗牌，正常洗牌保留特效、救援鱼和固定障碍。极端无解颜色分布会重新生成普通颜色并保留特效标记。

## 可选关卡元素（已实现并逐关引入）

| 元素   | 规则                                                                             | 首次出现   |
| ------ | -------------------------------------------------------------------------------- | ---------- |
| 冰块   | 1–3 层，消除上方动物减一层；冰层留在格子上，不随动物下落                         | 第 10 关   |
| 藤蔓   | 禁止该动物交换/下落；第一次命中只解除藤蔓，后续才能消除动物                      | 第 11 关   |
| 银币   | 按上面的银币通路规则                                                             | 第 14 关   |
| 鸡窝   | 固定障碍，每波最多敲击一次，两次击破；每个击破的窝产生两只小黄鸡                 | 第 19 关   |
| 小黄鸡 | 随补充动物落入棋盘，按黄色动物参与消除；同一波击破越多鸡窝，补充越多             | 随鸡窝出现 |
| 传送带 | 每完成两次有效交换，沿配置闭环移动一格，再结算新匹配；轨道被固定障碍阻塞时不移动 | 第 25 关   |
| 染色蛋 | 与特效交换清除染色蛋颜色；搭配直线附带该方向攻击，搭配爆炸附带 5×5 区域攻击      | 第 28 关   |

这些元素是独立关卡配置，不会全部堆到开场教学里。现有收集、清沙、救援主目标保持，障碍不自动成为额外通关目标。

## 动画时序

1. 两枚动物用平滑插值和轻微弧线完成约 210ms 交换。
2. 无效交换约 190ms 回弹，锁住重复输入，不改变棋盘和步数。
3. 普通消除约 210ms 缩小淡出；特效约 330ms，双魔力清屏约 560ms。
4. 下落约 350ms，按实际来源格绘制运动、各列轻微错开、落地轻弹。
5. 后续连锁逐波播放，不瞬间替换整张棋盘。
6. 传送带以约 360ms 平滑位移显示。

逻辑状态在有效交换后存档，视觉动画播放期间锁住输入；切后台或退出后可恢复结算后的状态，避免动画中断导致重复奖励。
